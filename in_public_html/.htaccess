<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Redirect everything to index.php inside 'public' folder
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/index.php [L]
</IfModule>
